#include "convert.hpp"

char valid_char(const std::string &arg) {
    if (arg.length() == 1)

    return ("Imposible");
}

int	argument_type(const std::string &arg) {
    std::cout << arg.length() << std::endl;
    // Carácteres imprimibles: del 30 al 126.
    int c = valid_char(arg);
    if ()
    return 1;
}
