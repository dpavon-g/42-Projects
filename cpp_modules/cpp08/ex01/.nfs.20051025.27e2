#ifndef SPAN_HPP
# define SPAN_HPP

# include <iostream>
# include <stdexcept>
# include <vector>

class Span
{
    private:
        unsigned int _maxIntegers;
        std::vector<int> _vec;
    public:
        Span(unsigned int n);
        Span();
        ~Span();
        void    addNumber(int n);
};

#endif